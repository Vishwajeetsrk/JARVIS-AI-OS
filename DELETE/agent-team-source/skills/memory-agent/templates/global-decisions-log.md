# Global Decisions Log

Append-only record of non-obvious decisions and why they were made, so they're never re-litigated from zero.

---

<!-- Example entry — delete once real entries exist
## 2026-02-02 Chose Next.js 15 + Supabase over separate Express backend for Learnify AI
- **Project:** Learnify AI
- **Context:** Needed to move fast on V1 with a solo team, but also needed row-level security for student data.
- **Chosen:** Next.js 15 App Router + Supabase (Postgres + RLS + Auth) as a single deployable unit.
- **Rejected alternatives:** Separate Express/Node API — rejected, doubles deployment surface and auth complexity for a solo founder at V1 stage.
- **Revisit if:** MRR crosses a threshold where a dedicated backend team exists, or Supabase RLS becomes a genuine bottleneck.
-->
